source "https://rubygems.org"

# Mesma stack usada pelo build "legacy" do GitHub Pages.
gem "github-pages", group: :jekyll_plugins

# A stdlib do Ruby >= 3.0 não traz mais webrick; o servidor de dev do Jekyll precisa dele.
gem "webrick"
